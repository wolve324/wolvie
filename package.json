{
  "name": "your-app-name",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://yourusername.github.io/your-repo-name",  // Replace with your actual GitHub username and repository name
  "dependencies": {
    // All your dependencies here (like react, react-dom, etc.)
  },
  "devDependencies": {
    "gh-pages": "^3.2.3"  // Ensure this is in your devDependencies
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  }
}
